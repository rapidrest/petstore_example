apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rrst.fullname" . }}-test-connection"
  labels:
{{ include "rrst.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "rrst.fullname" . }}']
  restartPolicy: Never
